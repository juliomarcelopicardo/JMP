VAR paco = 1

VARPACK camera {
  VAR x = 1
  VAR y = camera.x
  VAR z = 3
}

FUNC PrintExample(a,b,c) {
	PRINT(a)
	PRINT(b)
	PRINT(c)
  PRINT(a + b)
  PRINT(a - a)
  PRINT(a * c)
	PRINT(variable)


}

FUNC Example(first,second){
	VAR text = "This is going to be a simple message"
  VAR result = "End of the loop. iterator value : "
	
	VAR iterator = 0
	WHILE(4 < 5){		
		PRINT(text + first)
		PRINT(second)
		PRINT("Hello World")
		PrintExample(first, second, first)
    PRINT(" ")
    PRINT(result + iterator)
    PRINT(" ")
		iterator = iterator + 1
    RETURN
	}
  

}

FUNC Example2() {
  ExternalFunction(camera.x, camera.y, camera.z)

}

VAR pedro = camera.x

FUNC main() {
  VAR perico = pedro
  VAR pepe = 0
  WHILE(4 < 5){
    PRINT("PAUL PARRY")
    pepe = pepe + perico
    IF (pepe > 15) {
      Example2()
      RETURN
    }
    IF (pepe == 3) {
      PRINT("Equal")
    }
    IF (pepe != 3) {
      PRINT("NOT Equal")
    }
  }

}
